# Public Asset Layout

정적 리소스는 기능별 하위 폴더에 배치했습니다.

- `images/`: 텍스처와 이미지 리소스 (기존 `public/image`에서 이동)
- `models/`: FBX 및 참조 이미지 (기존 `public/model`에서 이동)
- `icons/`: 아이콘 리소스

필요한 경우 각 폴더 내부에서 추가 하위 디렉터리로 세분화할 수 있습니다.
